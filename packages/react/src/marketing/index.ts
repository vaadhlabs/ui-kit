// Marketing primitives — ported from @tensorcost/component-library in Phase 3.
// Per-component exports are appended as Phase 3a batches land.
// Orchestrator wires this barrel after both parallel agents commit their components.
export {};
