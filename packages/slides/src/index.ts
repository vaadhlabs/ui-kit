// TODO (Phase 5): React-to-image slide pipeline via @vercel/satori.
// This package will render React components to PNG/SVG frames that the
// marketing PPT pipeline can consume without a headless browser.
export {};
