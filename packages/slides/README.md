# @tensorcost/slides

WIP — Phase 5 of the ui-kit roadmap.

React components rendered to PNG via `@vercel/satori` for embedding into pptx slide decks via python-pptx. Tokens drive slide chrome + chart styles so generated decks visually match the app.

Not yet published to npm.
